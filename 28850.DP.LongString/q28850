[길이 제한 없이 스트링 입력하기]

길이에 제한 없이 스트링을 입력할 수 있는 함수 getLongString()을 작성하라.
프로토타입은
char * getLongString(); 
이며
최초 10바이트로 동적할당한 메모리를 이용하여 getchar를 이용하여 넣다가 메모리가 모자라면
두배 큰 메모리를 할당하고 현재의 내용을 복사한 후 계속하면 된다.

빈칸/tab/newline 은 건너 뛰고 빈칸이 아닌 것이 나오면 저장하기 시작해서 EOF나 빈칸, tab, newline 이 나오면 
스트링의 끝을 0으로 채우고 반환하면 된다.

main:

char *p;
p = getLongString();
printf("%s\n", p);

**  주의:  자율적으로 여러 버전을 등록한다. (각 버전에 대해서는 이 버전은 무엇을 하는 버전인지 간단히 설명) 
버전 개수에 제한은 없으나 최소 3개 버전 이상은 되어야 할 것으로 추측됨. 최종 프로그램만 등록하면 점수 없음.
